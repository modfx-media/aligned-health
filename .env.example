GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=ChIJk2H_u-Pr3IARsI2vkK-ZVaU

# Payload CMS — required for /admin and /api/*
PAYLOAD_SECRET=
# Neon pooled connection string (*-pooler.*, port 5432 is often blocked)
DATABASE_URL=
PREVIEW_SECRET=

# Public origin. On Vercel this must be https://alignedhealthoc.com (never localhost).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Optional. Without it, local /media uploads 404 in production.
BLOB_READ_WRITE_TOKEN=
